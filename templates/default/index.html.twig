{% extends 'base.html.twig' %}

{% block title %}POSTS{% endblock %}

{% block body %}
    {% for post in posts %}

<div class="example-wrapper" style="margin-top: 20px">
    <h1>Заголовок : {{ post.name }}</h1>
    <h1>Текст :  {{ post.description }}</h1>
    <h1>Дата публикации :  </h1>
</div>
    {% endfor %}
{% endblock %}
