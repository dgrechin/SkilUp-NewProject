{% extends 'base.html.twig' %}

{% block title %}POSTS{% endblock %}

{% block body %}
    {% for post in posts %}

<div class="example-wrapper" style="margin-top: 20px">
    <h1>Заголовок :<strong>{{ post.name }}</strong></h1>
    <h2>Текст :  {{ post.description }}</h2>
    <h7>Дата публикации : {{ post.createdAt| date("d.m.y   h:m:s") }}  </h7><br>
</div>
    {% endfor %}
{% endblock %}
